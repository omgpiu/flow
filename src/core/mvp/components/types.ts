export const enum Nodes {
    FINISH_NODE = 'finishNode',
    NOTE_NODE ='noteNode',
    CUSTOM_NODE = 'custom',
    TEXT_UPDATER = 'textUpdater',
    DEFAULT = 'default',
    INPUT = 'input',
    OUTPUT = 'output'
}
